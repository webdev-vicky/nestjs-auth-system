apiVersion: v1
kind: Service
metadata:
  name: nestjs-api-loadbalancer
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: nestjs-api
  ports:
    - protocol: TCP
      port: 81
      targetPort: 3000
